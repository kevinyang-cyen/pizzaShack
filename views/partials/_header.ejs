<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/">Pizza Shack</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    </div>
      <form class="form-inline" action="/order" method="GET">
        <div class="form-group mb-2">
          <label for="items-in-cart" style="margin-right: 10px"></label>
          <button type="submit" id= "cart-button" class="btn btn-outline-dark">View Cart (0)</button>
        </div>
      </form>
  </nav>
</header>

<script>
function updateCartState() {
  Object.entries(window.userCart).forEach(([pizzaName, quantity]) => {
    const slugName = slugify(pizzaName);
    const quantityLabel = $(`#cart-button`);
    const quantityInput = $(`#quantity-input-${slugName}`);
    quantityLabel.text(`View Cart (${count})`);
    quantityInput.val(quantity);
  })
}
</script>
